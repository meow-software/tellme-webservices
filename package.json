{
  "name": "tellme-webservices",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "prune": "npx turbo prune --scope=heimdall --out-dir=prune/heimdall --docker && npx turbo prune --scope=cerberus --out-dir=prune/cerberus --docker && npx turbo prune --scope=atlas --out-dir=prune/atlas --docker",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "turbo": "^2.5.6",
    "typescript": "5.9.2"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "npm@11.5.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
